double relativeToActualWidth({required double canvasWidth, required double relativeWidth}) {
    return relativeWidth * canvasWidth;
}

double relativeToActualHeight({required double canvasHeight, required double relativeHeight}) {
    return relativeHeight * canvasHeight;
}